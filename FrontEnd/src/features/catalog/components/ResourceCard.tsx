// TODO: resource card component
// Displays: name, resourceType, capacity, price (from linked ServiceOffering)
// Uses shadcn <Card> + MediaPhoto for image

export default function ResourceCard() {
  return null // TODO
}
