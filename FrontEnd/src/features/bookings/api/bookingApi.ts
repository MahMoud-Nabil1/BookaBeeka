// TODO: booking API functions
// createBooking(req: CreateBookingRequestDto, idempotencyKey?: string) → POST /api/bookings  [Header: Idempotency-Key]
// confirmBooking(bookingId, tenantId)                                   → POST /api/bookings/{bookingId}/confirm
// cancelBooking(bookingId, tenantId, reason, actorId)                   → POST /api/bookings/{bookingId}/cancel
// getBookingStatus(bookingId, tenantId)                                 → GET  /api/bookings/{bookingId}/status
// getCustomerBookings(customerId, tenantId)                             → GET  /api/bookings/customer/{customerId}

export {};
