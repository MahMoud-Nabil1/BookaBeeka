// TODO: date formatting utilities
// formatSlot(start: string, end: string)  → "Mon 14 Aug · 10:00–10:30"
// formatDate(iso: string)                 → localized date string
// formatDateTime(iso: string)             → localized date + time string

export {};
