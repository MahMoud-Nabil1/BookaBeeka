// TODO: define common/shared TypeScript interfaces

// ApiError           { timestamp, status, error, message }
// PaginatedResponse<T> { content: T[], page, size, totalElements, totalPages }

export {};
