// TODO: define inventory-related TypeScript interfaces

// Resource        { id, tenantId, branchId, name, resourceType, capacity, specs, isActive, isBookable }
// ServiceOffering { id, tenantId, branchId, name, price, durationMinutes, bufferMinutes, isActive }
// AttributeSchema { id, tenantId, entityType, fieldKey, fieldLabel, fieldType, isRequired, displayOrder }

export {};
