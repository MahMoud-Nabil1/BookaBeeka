// TODO: define booking-related TypeScript interfaces

// BookingStatus         = 'PENDING_PAYMENT' | 'CONFIRMED' | 'CANCELLED' | 'COMPLETED' | 'EXPIRED'
// CreateBookingRequestDto { tenantId, customerId, resourceId, start, end, totalAmount, currency }
// BookingConfirmationDto  { bookingId, status, lockId, createdAt }
// BookingDto              { bookingId, tenantId, customerId, resourceId, startTime, endTime, status, totalAmount, currency, cancellationReason, version, createdAt }
// BookingStatusDto        { bookingId, status, version }
// CancellationResultDto   { bookingId, refundAmount, refundPercentage, newStatus }

export {};
